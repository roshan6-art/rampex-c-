#include <stdio.h>
int main() {
    int a,b;
    scanf("enter a number a=%d and b=%d",a,b);
    if (a!=b)
    printf("the a and b are not equal\n");
    if(a==b)
    printf("the a abd b are equal\n");
    if(a>b)
    printf("the a is greater\n");
    if(a<b)
    printf("the a is lesser\n");
    else
    return 0;
}